<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Isaiah Jones | Online Coach</title>

  <!-- FULL CSS INSIDE THIS FILE -->
  <style>
    /* Base */
    *,
    *::before,
    *::after {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    :root {
      --bg: #050607;
      --bg-alt: #111218;
      --bg-soft: #181922;
      --gold: #f5c451;
      --gold-soft: #f8d47f;
      --text: #f7f7f7;
      --muted: #aaaaaa;
      --accent: #f5c451;
      --radius-lg: 16px;
      --radius-md: 12px;
      --radius-sm: 8px;
      --shadow-soft: 0 18px 45px rgba(0, 0, 0, 0.65);
      --transition-fast: 0.2s ease-out;
    }

    html {
      scroll-behavior: smooth;
    }

    body {
      font-family: "Poppins", Arial, sans-serif;
      background: radial-gradient(circle at top, #1b1c24 0, #050607 55%);
      color: var(--text);
      line-height: 1.6;
    }

    .container {
      width: min(1120px, 100% - 2.5rem);
      margin-inline: auto;
    }

    .section {
      padding: 5rem 0;
    }

    .section-alt {
      background: linear-gradient(135deg, #07080b, #111218);
    }

    .section-title {
      font-size: clamp(2rem, 3vw, 2.4rem);
      margin-bottom: 1rem;
      letter-spacing: 0.03em;
    }

    .section-subtitle {
      max-width: 620px;
      margin: 0 auto 2.5rem;
      color: var(--muted);
      text-align: center;
    }

    .center {
      text-align: center;
    }

    /* HEADER */
    header {
      position: sticky;
      top: 0;
      z-index: 20;
      backdrop-filter: blur(18px);
      background: linear-gradient(90deg, rgba(5,6,7,0.95), rgba(10,10,14,0.95));
      border-bottom: 1px solid rgba(245,196,81,0.12);
      padding: 0.75rem 0;
    }

    header .logo {
      text-decoration: none;
      color: var(--text);
      display: flex;
      flex-direction: column;
    }

    header .logo-main {
      font-weight: 600;
      letter-spacing: 0.12em;
      font-size: 0.95rem;
      text-transform: uppercase;
    }

    header .logo-sub {
      font-size: 0.7rem;
      color: var(--muted);
    }

    nav ul {
      list-style: none;
      display: flex;
      gap: 1.4rem;
    }

    nav a {
      text-decoration: none;
      color: var(--muted);
      letter-spacing: 0.06em;
      text-transform: uppercase;
      font-size: 0.86rem;
      transition: 0.2s;
    }

    nav a:hover {
      color: var(--accent);
    }

    /* HERO */
    .hero {
      min-height: 82vh;
      display: flex;
      align-items: center;
      padding: 4.5rem 0;
      background: radial-gradient(circle, rgba(245,196,81,0.15), transparent 55%);
      position: relative;
    }

    .hero-overlay {
      position: absolute;
      inset: 0;
      background: linear-gradient(135deg, rgba(0,0,0,.7), rgba(5,5,9,.9));
    }

    .hero-inner {
      position: relative;
      z-index: 3;
      display: grid;
      gap: 3rem;
    }

    .hero-text h1 {
      font-size: clamp(2.1rem, 3.4vw, 3rem);
      margin-bottom: 1rem;
    }

    .btn {
      padding: .7rem 1.25rem;
      border-radius: 999px;
      text-decoration: none;
      text-transform: uppercase;
      font-weight: 600;
      letter-spacing: .06em;
      display: inline-block;
      transition: .2s;
    }

    .btn-primary {
      background: linear-gradient(135deg, #f5c451, #f8d47f);
      color: #000;
    }

    .btn-ghost {
      border: 1px solid rgba(245,196,81,0.4);
      color: var(--gold-soft);
    }

    /* FOOTER */
    footer {
      padding: 2rem 0;
      background: #040405;
      border-top: 1px solid rgba(245,196,81,0.15);
      text-align: center;
      color: var(--muted);
      font-size: 0.85rem;
    }
  </style>
</head>

<body>

  <!-- HEADER -->
  <header>
    <div class="container" style="display:flex;justify-content:space-between;align-items:center;">
      <a class="logo" href="#">
        <span class="logo-main">Isaiah Jones</span>
        <span class="logo-sub">Coach · Trainer · Nutrition</span>
      </a>
      <nav>
        <ul>
          <li><a href="#about">About</a></li>
          <li><a href="#coaching">Coaching</a></li>
          <li><a href="#pricing">Pricing</a></li>
          <li><a href="#contact" class="btn-primary" style="padding:.5rem 1rem;">Apply</a></li>
        </ul>
      </nav>
    </div>
  </header>

  <!-- HERO -->
  <section class="hero">
    <div class="hero-overlay"></div>
    <div class="container hero-inner">
      <div class="hero-text">
        <h1>Transform your body now because it’s not leaving you any time soon.</h1>
        <p>Customized 1:1 coaching for muscle building, fat loss, and lifestyle results.</p>
        <div style="display:flex;gap:1rem;flex-wrap:wrap;">
          <a href="#contact" class="btn btn-primary">Apply for Coaching</a>
          <a href="#coaching" class="btn btn-ghost">View Programs</a>
        </div>
      </div>
    </div>
  </section>

  <!-- ABOUT -->
  <section class="section" id="about">
    <div class="container">
      <h2 class="section-title center">Meet Isaiah</h2>
      <p class="section-subtitle">
        ACE Certified Trainer · 4+ Years Experience · Crunch Fitness · Gold’s Gym
      </p>
      <p style="max-width:720px;margin:auto;color:#ccc;">
        I help clients build muscle, lose fat, and gain confidence with structured training and smart nutrition.
      </p>
    </div>
  </section>

  <!-- COACHING -->
  <section class="section-alt" id="coaching">
    <div class="container">
      <h2 class="section-title center">Coaching Options</h2>
      <p class="section-subtitle">Choose the support level you need.</p>
    </div>
  </section>

  <!-- PRICING -->
  <section class="section" id="pricing">
    <div class="container">
      <h2 class="section-title center">Simple Pricing</h2>
      <p class="section-subtitle">Pricing finalized during consultation.</p>
    </div>
  </section>

  <!-- CONTACT -->
  <section class="section-alt" id="contact">
    <div class="container">
      <h2 class="section-title">Contact</h2>
      <p>Email: <a href="mailto:isaiah0joness@gmail.com">isaiah0joness@gmail.com</a></p>
      <p>Instagram: <a href="https://instagram.com/I.jones_" target="_blank">@I.jones_</a></p>
    </div>
  </section>

  <!-- FOOTER -->
  <footer>
    © <span id="year"></span> Isaiah Jones Coaching
  </footer>

  <!-- FULL JS INSIDE SAME FILE -->
  <script>
    // Dynamic year
    document.getElementById("year").textContent = new Date().getFullYear();

    // Smooth scroll
    document.querySelectorAll('a[href^="#"]').forEach(a => {
      a.addEventListener("click", e => {
        const id = a.getAttribute("href");
        if (id.length > 1) {
          e.preventDefault();
          document.querySelector(id).scrollIntoView({ behavior: "smooth" });
        }
      });
    });
  </script>

</body>
</html>